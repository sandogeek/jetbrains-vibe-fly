一个 JetBrains IDE 优先的 vibe coding 插件。口号：Vibe coding on the fly。

## 架构

- Node.js：运行 Oh My Pi（omp）运行时，由插件启动并管理子进程
- 插件：界面基于 JCEF WebView
- Node.js ↔ JVM：stdio 仅承载 Agent 生命周期、健康检查和 WebSocket 会话控制的 SimpleRpc
- WebView ↔ Kotlin：通过 JCEF MessageRouter 承载宿主/IDE SimpleRpc
- WebView ↔ Node.js：通过 `127.0.0.1` 上带一次性票据的 WebSocket 承载 Agent 业务 SimpleRpc 和流式事件
- UI ↔ Agent 业务契约：由独立的 `vibefly-uiagent-shared` TypeScript 包共享，不再为这些接口定义 Kotlin 镜像
- WebView 前端：Vite + SolidJs + TypeScript + [SolidUI](https://www.solid-ui.com/)（Kobalte + Tailwind，组件源码拷贝至 `src/components/ui/`）+ Tailwind CSS v4（`@tailwindcss/vite`）；为规避 JCEF 无法加载 jar 包内 css/js，注册自定义 Scheme/Resource Handler（如 `http://vibefly/`），将 classpath 内资源以 HTTP 语义流式服务给 WebView，保留 Vite 默认多 chunk 输出、代码分割与懒加载（不使用 vite-plugin-singlefile 全量内联，避免首屏整包解析卡顿）
- 开发热更：classpath scheme 无法代理 WebSocket，故 dev 时 JCEF 直连 Vite（`http://127.0.0.1:5173/`），由 `-Dvibefly.ui.dev=true` / `-Dvibefly.ui.dev.url=...` 切换；生产仍走 `http://vibefly/`
- 引擎依赖：基于 [Oh My Pi](https://github.com/can1357/oh-my-pi)（`@oh-my-pi/*` npm 包）构建；定制通过依赖版本、扩展/hooks、或本仓 `vibefly-*` 适配层完成

### 核心技术栈

- Kotlin/Java：主机端，负责 JVM、Node.js/omp 与 WebView 的生命周期、控制面和 IDE 能力
- Node.js：运行 Oh My Pi agent，处理代码分析与生成任务
- Oh My Pi（omp）：vibe coding 引擎（会话、工具、模型、子代理、LSP/DAP 等）
- CEF (Chromium Embedded Framework)：WebView 容器，渲染 Vibe Coding UI
- SimpleRpc：统一双向 RPC 语义、类型生成、超时与取消
- stdio transport：JVM 与 Node.js 之间传输控制面 SimpleRpc 消息
- WebSocket transport：WebView 与 Node.js 之间传输 Agent 业务 SimpleRpc 消息
- JCEF transport：JVM 与 WebView 之间传输 SimpleRpc 消息

### SimpleRpc over stdio 控制面约定

- JVM 通过子进程 `stdin` 向 Node.js 发消息，Node.js 通过 `stdout` 向 JVM 发消息；这里只承载生命周期和 WebSocket 会话控制。
- `stdout` 仅用于 SimpleRpc 协议数据，Node.js 日志统一写 `stderr`，避免污染协议流。
- SimpleRpc 消息使用 UTF-8 JSON；stdio transport 用 `Content-Length` 描述负载字节数，不依赖换行分割。
- SimpleRpc 负责请求、响应、错误、取消；stdio transport 负责分帧、有序传输与背压。
- 实现：`StdioRpcTransport`（Kotlin）+ `createStdioSimpleRpc` / `createStdioTransport`（Node stream，`@sandogeek/simple-rpc-node`）。
- Node.js 异常退出或 stdio 关闭时，JVM 应立即结束会话并使未完成请求失败（`onClosed`）。
- Agent 业务消息走 WebSocket；大型本地文件优先传路径、范围和校验信息，避免大量二进制经 Base64 放入 JSON。

## 功能

- 支持将文件拖拽到输入框用于添加上下文
- 选中代码后的 Floating ToolBar 支持将选中内容添加到上下文
### commit
- 给commit功能提供更多上下文,方式文件夹内包含文件vibefly.commit.md,从变更文件找最近的这个文件作为上下文提供给llm？

### 设置
当前存在问题，不同VibeflyBrowserPanel之间状态不是共用的，设置的数据没有自动同步

## roadMap
- 支持多套主题色，其中默认主题色为跟随IDE主题
- 沙箱支持
- 支持在 IDEA 中调试 Node.js？
- 设置里面增加可选provider、可选模型设置，chat中候选只出现这两者的合集？

## 开发指南

- 上游 Oh My Pi 以 npm 依赖接入；若必须 patch 上游，优先用包管理器 patch / 独立 patch 目录，并在变更处注明来源与原因
- 本仓自有包文件夹以 `vibefly-` 开头
- 与 Oh My Pi 的适配、桥接、插件侧封装放在本仓 `vibefly-*` 或 `agent` 等自有模块中，不混入上游包路径

## 日志栈（TypeScript）

- `vibefly-agent`：winston → **stderr only**（`VIBEFLY_LOG_LEVEL`，默认 info）
- `vibefly-ui`：loglevel → console → `bindConsoleToHost` → `logFromWeb`（`localStorage.vibefly.log.level` / `VIBEFLY_LOG_LEVEL`）

ui界面：https://elements.ai-sdk.dev/
