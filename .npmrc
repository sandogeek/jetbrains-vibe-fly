auto-install-peers=true
strict-peer-dependencies=false

# Gradle / IDE-launched pnpm has no TTY. Without this, a lockfile/node_modules
# mismatch (e.g. removing a workspace dep) aborts with
# ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY during `pnpm run`.
confirmModulesPurge=false
